```text
/goal /caveman ultra
Read AGENTS.md/CLAUDE.md. Work only in D:\dev\rohkai.
Run: pwsh -NoProfile -ExecutionPolicy Bypass -File D:\dev\rohkai\scripts\preflight-context.ps1
Read svg-zero-dep skill. Read: docs/SVG_RENDERER_ROADMAP.md (Post-R8 lanes),
docs/SVG_PRECISION_AND_BENCH.md (fuzz tiers), src/canvas/svg_rasterizer.rs (R8.1 harness:
fuzz_rng/fuzz_mutate/fuzz_drive/fuzz_run/fuzz_iters_from_env, ROHKAI_FUZZ_ITERS, seed corpus),
tests/fixtures/svg_fuzz/, src/codegen/export.rs (single-crate:: contract).

Scope note: OPTIONAL hardening lane (depends R8.1). Does NOT block R9-R12 — run anytime. This turns the
R8.1 in-repo fuzz harness into a deeper, continuous, still-zero-dependency capability. NO cargo-fuzz /
libfuzzer / arbitrary / honggfuzz — pure in-repo, deterministic, seeded.

Before coding, derive + REPORT from code:
1. exact R8.1 harness shape + what byte-mutation covers vs misses (structure-aware gaps in path/XML/PNG/JPEG)
2. how the seed corpus loads today (include_str!/consts) vs a directory-driven loader
3. where a crash-repro buffer would be captured (ignored test only; no fs in shipped code paths)
4. tests per path

Required:
1. Structure-aware mutators ALONGSIDE byte mutation, all deterministic + seeded: path-token-level edits
   (drop/dup/replace commands+coords), XML-token edits (tag/attr/quote tamper), PNG chunk-level edits
   (length/CRC/type tamper), JPEG marker/segment-length edits. Mix with existing byte mutator by PRNG.
2. Corpus management: directory-driven seed loader that reads every file under tests/fixtures/svg_fuzz/
   (bounded count+size) so new seeds drop in without code edits; document a crash-repro capture workflow
   (the ignored test may write a failing buffer to a dev path — never in shipped render paths).
3. Release deep-fuzz methodology: document the canonical commands + budgets (ROHKAI_FUZZ_ITERS tiers under
   --release); add an optional CI nightly recipe as a doc/artifact (NEVER a runtime/Cargo dep).
4. Optional coverage: document dev-only coverage via the toolchain's built-in instrumentation
   (-C instrument-coverage), no new crate; report which decoders the corpus exercises.
5. Keep the always-run smoke; both embedded sources std-only; single-crate:: export contract still passes;
   honor all existing caps; no banned crate names in src/ (validate-svg-import.ps1 greps src/).

Tests:
1. each structure-aware mutator is deterministic (same seed -> same output) and bounded
2. directory seed loader is bounded (count/size) and tolerates junk files without panic
3. smoke fuzz still green; a short deep sweep (env-set small count) runs clean
4. export parity: ignored all-built-in exported-project cargo check still passes

Verify (zero warnings): cargo fmt --check; cargo check; cargo test; cargo clippy --all-targets -- -D warnings;
pwsh scripts\validate-svg-import.ps1; pwsh scripts\check-text-encoding.ps1;
ROHKAI_FUZZ_ITERS=2000 cargo test --release --bin rohkai -- --ignored fuzz_decoders_no_panic_bounded.

Docs: update docs/SVG_PRECISION_AND_BENCH.md (structure-aware mutators + corpus loader + coverage +
nightly recipe); add an R8.2 row to SVG_RENDERER_ROADMAP.md gap matrix/lanes (optional hardening, parallel
to R9); update svg-goal-plan-prompts/README.md; append CODE_COOP.md + DEVLOG.md.

Final report: mutators added, corpus/coverage workflow, verification numbers, gaps only if excluded.
Success: deterministic structure-aware fuzzing + directory corpus + documented release/nightly/coverage
workflow, all zero-dep, smoke green, zero warnings. Optional lane — main order stays R9 -> R12.
```
