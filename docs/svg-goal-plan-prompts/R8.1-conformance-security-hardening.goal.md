```text
/goal /caveman ultra
Read AGENTS.md/CLAUDE.md. Work only in D:\dev\rohkai.
Run: pwsh -NoProfile -ExecutionPolicy Bypass -File D:\dev\rohkai\scripts\preflight-context.ps1
Read svg-zero-dep skill. Read: docs/SVG_RENDERER_ROADMAP.md (Post-R8 lanes = truth),
src/canvas/svg_rasterizer.rs (parser, path tokenizer, PNG/JPEG decoders, inflate, caps, golden harness ref),
src/svg_core.rs, src/canvas/svg_golden.rs, src/codegen/export.rs (single-crate:: contract).

Goal: implement R8.1 conformance + security hardening. No new crates. Make the hand-rolled parsers/decoders
provably safe on hostile input and validate claimed features against a curated corpus.

Before coding, derive + REPORT from code:
1. every byte-consuming parser/decoder reachable from untrusted SVG (XML scan, path tokens, base64, zlib/
   DEFLATE inflate, PNG chunk, JPEG marker/entropy) + their current caps
2. existing golden harness + ignored benchmark/oracle; what's covered vs not
3. which "supported" features have a golden/unit test vs only code
4. tests+fixtures per path

Required:
1. In-repo fuzz harness (no cargo-fuzz dep): a deterministic, seedable byte-mutation generator + a runner
   that feeds mutated SVG/PNG/JPEG/path/inflate inputs through the decoders asserting NO panic, bounded
   output, and Err-or-bounded-Ok. Gated #[ignore = "fuzz"] with a fixed seed corpus checked into
   tests/fixtures/svg_fuzz/ so a CI run is reproducible. Add a small always-run smoke (few iterations).
2. Curated W3C SVG 1.1 sub-corpus as ASCII goldens (geometry/paint/clip/mask/filter/image) filling any
   feature lacking a visual test; every supported feature gets one. Dependency-free.
3. Dev-only external-oracle diff: extend the existing ignored oracle test to document the workflow (external
   renderers are CI artifacts/dev-only, NEVER runtime deps; do NOT write banned crate names in src/ —
   validate-svg-import.ps1 greps for them).
4. Benchmark methodology: document parse/scene-build/raster/peak-alloc budgets next to the ignored bench;
   add memory-cap regression tests (oversized dims/pixels/inflate -> bounded fail+diagnostic).
5. Rendering-precision policy doc: coverage grid, nearest sampling, premultiplied vs sRGB vs linearRGB
   boundary (note current filters run sRGB — flag as the R10 conformance gap).
6. both embedded sources std-only; single-crate:: export contract still passes.

Tests:
1. fuzz smoke (always-run) + ignored full fuzz over seed corpus -> no panic, bounded
2. expanded golden determinism across the new corpus
3. memory/CPU cap regressions for each decoder
4. export parity: ignored all-built-in exported-project cargo check still passes

Verify (zero warnings): cargo fmt --check; cargo check; cargo test; cargo clippy --all-targets -- -D warnings;
pwsh scripts\validate-svg-import.ps1; pwsh scripts\check-text-encoding.ps1; cargo test -- --ignored (fuzz+bench).

Docs: flip R8.1 tasks to [x] in SVG_RENDERER_ROADMAP.md + reconcile Tests/Conformance gap rows; update
feature-evaluation; append CODE_COOP.md handoff + DEVLOG.md entry (next=R9).

Final report: paths+fixtures added, fuzz/corpus/bench coverage, verification numbers, gaps only if excluded
before editing. Success: parsers/decoders fuzz-clean + bounded, every supported feature has a visual test,
benchmark+precision policy documented, zero warnings, no new deps. Next: R9.
```
