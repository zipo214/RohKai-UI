name: Windows Build

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    name: Build Windows executable
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust
        # SHA-pinned for supply-chain hardening. When pinned to a SHA the action
        # cannot infer the toolchain from its ref, so set it explicitly.
        uses: dtolnay/rust-toolchain@6190aa5fb88a88ee71c12769924bbe63a9ab152e # 1.96.0
        with:
          toolchain: 1.96.0
          components: clippy,rustfmt

      - name: Check toolchain and export version alignment
        shell: pwsh
        run: ./scripts/check-toolchain-alignment.ps1

      - name: Check formatting
        run: cargo fmt --check

      - name: Test
        run: cargo test

      - name: Clippy
        run: cargo clippy --all-targets -- -D warnings

      - name: Build release executable
        run: cargo build --release

      - name: Upload executable artifact
        uses: actions/upload-artifact@v4
        with:
          name: rohkai-windows-exe
          path: target/release/rohkai.exe
